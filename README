The index example needs a working http-server.
To see the isogameengine in action you could start the node server:

1. surf to project folder with your console
2. type 'node http_server'
3. open in browser: 'localhost:8080/index.html'

